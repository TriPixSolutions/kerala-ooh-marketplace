import Link from "next/link";
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
const money=(n:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(n||0);
export default async function OwnerAnalyticsPage(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/auth/login");const{data:p}=await s.from("profiles").select("full_name,role,status").eq("id",user.id).single();if(!p||p.role!=="LISTING_OWNER"||p.status!=="ACTIVE")redirect("/dashboard");const[{data:l},{data:i},{data:pay},{data:b},{data:c},{count:e}]=await Promise.all([s.from("ad_listings").select("id,title,status").eq("owner_id",user.id).order("created_at",{ascending:false}),s.from("invoices").select("id,total_amount,amount_paid,status,invoice_number,title,created_at").eq("owner_id",user.id).order("created_at",{ascending:false}),s.from("payment_transactions").select("id,amount,status,created_at").eq("owner_id",user.id).order("created_at",{ascending:false}),s.from("booking_requests").select("id,status,created_at").eq("owner_id",user.id).order("created_at",{ascending:false}),s.from("campaigns").select("id,status,start_date,end_date").eq("owner_id",user.id).order("created_at",{ascending:false}),s.from("enquiries").select("id",{count:"exact",head:true}).eq("owner_id",user.id)]);const collected=(pay??[]).filter(x=>x.status==="CAPTURED").reduce((a,x)=>a+Number(x.amount||0),0);const invoiced=(i??[]).reduce((a,x)=>a+Number(x.total_amount||0),0);const outstanding=Math.max(invoiced-(i??[]).reduce((a,x)=>a+Number(x.amount_paid||0),0),0);const activeSpaces=(l??[]).filter(x=>x.status==="PUBLISHED").length;const activeCampaigns=(c??[]).filter(x=>x.status==="ACTIVE").length;const openBookings=(b??[]).filter(x=>["REQUESTED","PENDING_OWNER","ACCEPTED"].includes(x.status)).length;return <main className="min-h-screen bg-[var(--surface)] text-[var(--ink)]"><header className="border-b border-[var(--border)] bg-white"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 lg:px-8"><Link href="/owner" className="text-[15px] font-semibold tracking-[0.16em]">YOUR AD SPACE</Link><Link href="/revenue" className="text-sm text-[var(--slate)]">Revenue</Link></div></header><div className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><p className="text-xs uppercase tracking-[0.16em] text-[var(--muted)]">Owner performance</p><h1 className="mt-2 text-4xl font-semibold tracking-[-0.04em]">Your space performance.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-[var(--slate)]">Understand inventory, demand, booking activity and money collected from your advertising spaces.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Collected",money(collected)],["Outstanding",money(outstanding)],["Published spaces",activeSpaces],["Open enquiries",e??0]].map(([a,v])=><div key={a} className="rounded-2xl border border-[var(--border)] bg-white p-6"><p className="text-sm text-[var(--muted)]">{a}</p><p className="mt-3 text-3xl font-semibold">{v}</p></div>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-3"><div className="rounded-2xl border border-[var(--border)] bg-white p-6"><p className="text-xs uppercase tracking-[0.14em] text-[var(--muted)]">Bookings</p><p className="mt-3 text-3xl font-semibold">{openBookings}</p><p className="mt-2 text-sm text-[var(--slate)]">Active booking requests</p></div><div className="rounded-2xl border border-[var(--border)] bg-white p-6"><p className="text-xs uppercase tracking-[0.14em] text-[var(--muted)]">Campaigns</p><p className="mt-3 text-3xl font-semibold">{activeCampaigns}</p><p className="mt-2 text-sm text-[var(--slate)]">Currently active</p></div><div className="rounded-2xl border border-[var(--border)] bg-white p-6"><p className="text-xs uppercase tracking-[0.14em] text-[var(--muted)]">Portfolio</p><p className="mt-3 text-3xl font-semibold">{l?.length??0}</p><p className="mt-2 text-sm text-[var(--slate)]">Total advertising spaces</p></div></div><section className="mt-6 rounded-2xl border border-[var(--border)] bg-white"><div className="border-b border-[var(--border)] px-6 py-4"><h2 className="font-semibold">Recent invoices</h2></div>{(i??[]).slice(0,10).map(x=><div key={x.id} className="grid gap-3 border-b border-[var(--border)] px-6 py-4 last:border-0 sm:grid-cols-[1fr_130px_140px]"><div><p className="text-sm font-medium">{x.invoice_number}</p><p className="mt-1 text-xs text-[var(--slate)]">{x.title||"Advertising invoice"}</p></div><span className="text-xs font-medium">{x.status}</span><span className="text-sm font-semibold sm:text-right">{money(Number(x.total_amount))}</span></div>)}{!(i??[]).length&&<p className="p-6 text-sm text-[var(--slate)]">No invoices yet.</p>}</section></div></main>}
